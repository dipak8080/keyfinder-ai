annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.0
audioread==3.1.0
certifi==2025.11.12
cffi==2.0.0
charset-normalizer==3.4.4
click==8.3.1
decorator==5.2.1
essentia==2.1b6.dev1389
fastapi==0.127.0
h11==0.16.0
idna==3.11
joblib==1.5.3
lazy_loader==0.4
librosa==0.11.0
llvmlite==0.46.0
msgpack==1.1.2
numba==0.63.1
numpy==2.3.5
packaging==25.0
platformdirs==4.5.1
pooch==1.8.2
pycparser==2.23
pydantic==2.12.5
pydantic_core==2.41.5
python-multipart==0.0.21
PyYAML==6.0.3
requests==2.32.5
scikit-learn==1.8.0
scipy==1.16.3
six==1.17.0
soundfile==0.13.1
soxr==1.0.0
starlette==0.50.0
threadpoolctl==3.6.0
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.2
uvicorn==0.40.0

# ---------- YOUTUBE / TIKTOK EXTRACTION ----------
yt-dlp[default]==2026.07.04
bgutil-ytdlp-pot-provider==1.3.1
# Browser TLS/JA3 impersonation for yt-dlp. Required by the TikTok
# extractor, which otherwise warns "attempting impersonation, but no
# impersonate target is available" and works only until TikTok tightens
# its anti-bot checks - unlike YouTube there is no client ladder to fall
# back on there. Also gives yt-dlp an extra request handler for YouTube's
# web-family clients.
curl-cffi==0.15.0

# ---------- SEPARATION (Demucs) ----------
# CPU-only torch build - the default `pip install torch` often pulls a
# CUDA build (~2GB+) with GPU libraries this VPS (no GPU) will never use.
# Installing from PyTorch's dedicated CPU wheel index avoids that entirely
# - meaningfully smaller download, faster install, less disk usage.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchaudio==2.5.1+cpu
demucs==4.0.1

faster-whisper